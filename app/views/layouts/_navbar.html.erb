<nav class="navbar navbar-expand-lg shadow-sm">
  <div class="container">
    <%= link_to (user_signed_in? ? authenticated_root_path : unauthenticated_root_path), class: "navbar-brand d-flex align-items-center gap-2" do %>
      <span class="btn btn-primary btn-sm rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; padding: 0;">
        💲
      </span>
      <span>FinControl</span>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarPrincipal">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarPrincipal">
      <ul class="navbar-nav mx-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Dashboard", dashboard_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Relatórios", reports_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav ms-auto align-items-lg-center">
        <% if user_signed_in? %>
          <li class="nav-item me-2">
            <span class="nav-link">Olá, <%= current_user.name %></span>
          </li>
          <li class="nav-item">
            <%= link_to "Sair", destroy_user_session_path,
              data: { turbo_method: :delete },
              class: "btn btn-outline-secondary btn-sm" %>
          </li>
        <% else %>
          <li class="nav-item me-2">
            <%= link_to "Entrar", new_user_session_path, class: "btn btn-link text-decoration-none" %>
          </li>
          <li class="nav-item">
            <%= link_to "Criar conta", new_user_registration_path, class: "btn btn-primary btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>